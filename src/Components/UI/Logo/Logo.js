import React from 'react';
import classes from "./Logo.module.css"

const Logo = () => {
    return (
        <div className={classes.logo}>
            <h3>Placeholder</h3>
        </div>
    );
};

export default Logo;